// db.js
/*const { Pool } = require('pg');


const dbkastello = new Pool({
    host: "localhost",
    user: "postgres",
    port: 5432,
    password: "khedma123",
    database: "kastello"
});
dbkastello.connect(function (error) {
    if (error) {
        console.error("Error connecting to Database:", error);
    } else {
        console.log("Successfully connected to Database Kastello");
    }
});

module.exports = dbkastello;
*/